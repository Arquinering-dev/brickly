## Sesión 2026-06-24

### Objetivo
Ajustes en el dashboard (Control Presupuesto). Track Excel queda para después ("primero el dashboard").

### Completado (solo dashboard_v2/ — Excel intacto, no requiere recalc)
- [x] **Bloques GGBB con formato de control completo.** Gastos Generales / Directos / Indirectos
  pasan de la tabla vieja (Concepto + Presupuestado + % del bloque, con un bloque de gasto Tezamat
  aparte) a la **misma tabla que Costo de Obra**: columnas Presupuestado · Gastado · Saldo · Consumo ·
  Desvío (sin "Avance s/cert", que no aplica). Cada línea de `1_GGBB` es una fila; consumo/desvío
  con semáforo igual que obra.
- [x] **Gasto de los bloques GGBB = doble fuente** (confirmado con Pedro, opción "ambas"):
  `2_Gastos_DirInd` (Coordinación, Proyecto, Payroll, Vehículo) **+** cuentas indirectas de Tezamat
  (rama 51/52, ej. Internet, Aguas, Honorarios, Gestoría). Cada gasto se mapea a su **línea de
  presupuesto** por nombre normalizado; los renombres conscientes viven en `GASTO_A_LINEA`
  (`read_obra.py`): hoy sólo `Payroll - Administración → Admin (50%Compras - 50%RRHH)`.
  Gasto sin línea → fila **"sin ppto"** en su sección (ej. Payroll Socios, Honorarios sueltos).
- [x] **Bloque por sección de `1_GGBB`** (no por la columna Tipo de `2_Gastos_DirInd`): Proyecto (50%)
  cae en Generales aunque el egreso esté tagueado "Directo", para reconciliar línea a línea. La col
  Tipo queda sólo para el split Directo/Indirecto del cash flow.
- [x] **Filas = todas las líneas presupuestadas (`1_GGBB` Subtotal > 0)**, con Gastado 0 si todavía no
  hay egreso, igual que el bloque de obra.
- [x] **Ordenamiento de rubros de obra**: por etapa (fila de `1_Presupuesto`), **agrupando MT/MO del
  mismo rubro** (anclado a la primera aparición del rubro) y poniendo **MO antes que MT**
  (`_orden_obra` en `read_obra.py`).

### Archivos tocados
- `dashboard_v2/reader/read_obra.py`: `_ggbb_secciones` (devuelve `lineas` con `norm`), `_control_ppto`
  (nueva firma con `gdi`; gasto dual; bloques GGBB con `rubros`; `_orden_obra`), `_ggbb_rubro`,
  `GASTO_A_LINEA`, y `_parse` (carga `gdi` antes del control).
- `dashboard_v2/web/js/ds.js`: `ggbbCard` reescrito a tabla estilo obra.

### Cambio de contrato JSON (consultado vía AskUserQuestion antes de implementar)
Los bloques `control_ppto.bloques[tipo='ggbb']` pasan de `{items, gasto_cuentas}` a `{rubros}` (misma
forma que el bloque obra, sin `avance_cert_pct`). El top-level `control_ppto.rubros` (obra) y los
subtotales NO cambian → frontend legacy y dashboard summary intactos. Verificado: Flask 200 en CH/GDR/SVD
+ screenshot de `/ds/obra/CH/control` OK.

### Pendiente para próxima sesión / datos
- Ver `pendientes.md` (typo "Homigón", mapeo Edenor/Aysa, Payroll Socios sin línea de ppto).

---

## Sesión 2026-06-24 (parte 2) — Estandarización de `1_Presupuesto` (track Excel)

### Objetivo
Unificar el formato de `1_Presupuesto` en las 3 obras: distinguir presupuestos (PTO 01/02),
estandarizar columnas de costo/venta, y decidir/eliminar la redundancia del avance físico por tarea.

### Auditoría (antes de tocar nada)
- 2 layouts distintos: CH (v8_11, viejo) vs GDR/SVD (v8_12, idénticos entre sí, más rico).
- Convención de venta divergente: **CH/GDR = costo×coef único** (coef en `P2`); **SVD = input por tarea**.
- Avance físico: TODO derivado de `Cert_App_Output`; GDR/SVD arrastran un bloque manual legacy muerto.
- Acoplamiento real (con refs de **columna completa**, que un primer scan perdió): `0_CONFIG`,
  `_Listas`, `Cert_App_Output.I` (PV total tarea ← PV subtotal, GDR/CH), `Cert_Control_OC.E`,
  `1_Composicion` (CH). CH `0_CONFIG` usa **ArrayFormula**.

### Decisiones (confirmadas con Pedro, 4 preguntas)
- **Layout único** A:X en las 3 obras (base = GDR/SVD; CH reescrito).
- **Venta = input por tarea** en TODAS (unifica el modelo; soporta markup no uniforme). CH/GDR se
  congelan desde el coef (value-preserving exacto). Coef queda en `1_GGBB` como referencia.
- **Avance reducido a `% Acum Tot`** (1 columna derivada). Eliminado el resto (~14 cols). Se conserva
  esa 1 porque permite a `Cert_Control_OC!E` reproducir EXACTO el % avance físico ponderado por PV
  del ppto (necesario por SVD, cuyo circuito usa PV del documento ≠ PV del ppto).
- **PV subtotal** = 1 columna. Aplicado a **las 3 de una**.

### Completado
- [x] `scripts/estandarizar_presupuesto_v8.py` — reconstruye `1_Presupuesto` al layout canónico
  **preservando el número de fila** de cada tarea, y recablea los consumidores por **remapeo de
  columnas** (un solo pase; maneja `str` y `ArrayFormula`).
- [x] CH → `v8_12`, GDR → `v8_13`, SVD → `v8_2`. recalc 0 + COM cacheado en las 3.
- [x] **Value-preserving EXACTO** verificado vs viejo recalculado: CONFIG (costo controlable/total/
  venta), `Cert_App_Output.I` sum, % avance físico (E) y certificado (F) por OC — sin cambios.
- [x] Reader+drilldown del dashboard: `cert$` y avance per-rubro = `% Acum Tot × PV subtotal`
  (`read_obra._presupuesto`, `drilldown.etapa_detail`). `obras.yaml` → versiones nuevas.
- [x] Dashboard verificado: Flask 200 en las 3, drilldowns (rubro 19 tareas, etapa PTO01 100%) OK,
  screenshot del Control de GDR correcto.
- [x] Docs: `HANDOFF_flujo_resumen_v8.md` §2.1 (layout estándar) + §6 actualizada; `CLAUDE.md`
  (tabla de obras + decisión); este log.

### Lecciones
- Scan de acoplamiento DEBE capturar refs de **columna completa** (`1_Presupuesto!$Z:$Z`) — no
  exigir dígito tras la columna.
- `ArrayFormula` no es `str`: tratarla aparte al escanear/remapear fórmulas (CH `0_CONFIG`).
- `excel_recalc.py` lanza `TypeError` en `wb.Close(SaveChanges=False)` (guarda igual antes de cerrar;
  conviene revisar/fix para no dejar Excel huérfano).

### Enmienda (mismo día) — columna `Margen` para auditar el margen por tarea
Pedro observó que congelar la venta a valores oculta el margen usado por tarea. Evaluadas las
alternativas (venta=Costo×K input · varios K en `0_CONFIG` · venta input + Margen derivado):
- **Elegido: venta = input (fuente de verdad estable) + columna `Margen` = `=IFERROR(PV/Costo Unit,"")`
  derivada.** Razones: margen verificable por tarea sin atar la venta al costo (protege la base de
  certificación que lee el PV subtotal); maneja la tarea venta-sin-costo (SVD `19.17`); value-preserving
  (la venta no cambia). Descartado "varios K en CONFIG": SVD varía por tarea de forma casi continua
  (7 coefs), no en tiers.
- Layout pasa a 25 cols A:Y. Regeneradas las 3 (recalc 0 + COM). Sanity: `Costo×Margen=Venta` en
  todas las filas; SVD expone sus 7 coeficientes reales (1,2569 ×146 · 1,3952 ×11 · 2,0571 ×2 …).
- (Opcional futuro: surface del Margen en el dashboard — el dato ya está en el Excel.)

### Parte 3 — Margen en el dashboard (implementado)
Confirmada la vista (Control Ppto: tabla de Costo de Obra + drawer de tareas). Implementado:
- Reader (`read_obra._presupuesto`): `margen_pct` por rubro MT (ΣPV÷ΣCosto, "—" en MO) + `margen_obra`
  (global, referencia de color). `_control_ppto` los propaga (`control_ppto.rubros[].margen_pct`,
  `control_ppto.margen_obra`). Aditivo.
- Drilldown (`_presupuesto_tareas`): `margen` (PV÷Costo de la tarea) + `pv_tarea` por tarea.
- Frontend (`ds.js`+`ds.css`): columna **"Margen ppto"** en Costo de Obra y **"Margen"** en el drawer
  de tareas; `margenCell`/`margenDotLvl` colorean relativo al margen global de la obra (<1 rojo · <ref
  ámbar · ≥ref verde). Margen **presupuestado**, etiquetado como tal (no rentabilidad realizada).
- Verificado: SVD expone variación real por rubro (Preliminares 1,26 · GG 1,60 · Hormigón MT 1,38) y
  por tarea; CH/GDR uniforme (1,3565 / 1,3327). Flask 200 en todas las vistas; screenshot OK.
- Antes: revisado que el dashboard levante bien lo impactado por la estandarización del Excel —
  composición de insumos (CH XLOOKUP recableado: 57 insumos, 21/21 reconcilian), jornales, KPIs,
  drill de etapa/rubro: todo OK. SVD sin composición es pre-existente (no por el cambio).
- Umbral de color = margen global de la obra (auto-calibra). Si Pedro quiere un **target fijo** de
  margen, se cambia `_margenRef` / `margenDotLvl` en `ds.js`.

### Parte 4 — Drawer de drill más ancho (sin scroll horizontal)
El drawer (640px) cortaba las tablas anchas (tareas+insumos, etc.) → scroll horizontal. Causa doble:
ancho chico + `td:first-child{white-space:nowrap}`. Fix (CSS, aplica a TODOS los drills por compartir
el shell `.ds-drawer`): ancho = `min(1100px, max(50vw, 620px))` (≈½ pantalla, capeado en monitores
anchos) + en el drawer las descripciones cortan línea (`.ds-drawer .ds-table td:first-child{white-space:normal}`)
+ `overflow-x:hidden`. Verificado: se ve toda la tabla (incl. Margen) sin scroll horizontal. Ancho
confirmado por Pedro (50vw, tope 1100px).

### Parte 5 — Vista "Avance y Certificaciones" reestructurada
Consulta previa resuelta (punto 6): el circuito separa **Gap A** (avance del JO sin certificar =
`Cert_Control_OC` col J "$ Avance s/certificar" + sección "PENDIENTES DE CERTIFICAR") y **Gap B**
(certificado sin facturar = `Certificado − Facturado`, col K). Pedro pidió **una card para cada uno**.
Layout nuevo de KPIs:
- **Fila 1 (3 cards):** Avance físico (subtítulo = `X de N tareas al 100%`, con ⓘ que explica el
  ponderado vs conteo) · Certificado de avance (`más $X de anticipo · Total = $Y`) · **Total facturado**
  (B/N debajo).
- **Fila 2 (4 cards):** Avance pendiente de certificar (Gap A) · Cert. pendiente de facturar (Gap B) ·
  Facturado pendiente de cobro (B/N) · Cobrado (B/N).
- Eliminadas: card "Última certificación" y el pipe de 3 pasos.

Cambios reader (aditivos):
- `read_obra._presupuesto`: cuenta `tareas_completas`/`tareas_total` (tarea = código `N.NN` con punto;
  excluye banners de etapa cuyo cod es entero "1","2"…). 100% = `% Acum Tot ≥ 0,999`. Verificado:
  CH 3/181 · GDR 42/191 · SVD 45/191 (totales matchean al estandarizador).
- `cert.py`: `_control_oc` lee col J → `avance_sin_certificar` (Gap A, clamp ≥0); `_facturacion` lee
  `tipo`; `read_cert` arma `facturado_bn`/`cobrado_bn` (split B/N desde Cert_Facturacion, Anticipo aparte).
- Frontend: helper `bnPct` (proporción B/N); `dsKpi` con `tip`. Flask 200 en todas las vistas; screenshot OK.

⚠ Dato de realidad: `Facturado`=0 en las 3 obras (facturación/cobros no cargados aún → `pendientes.md`),
por eso Total facturado / Cobrado / proporciones B/N salen en $0 / "sin facturación cargada", y
Cert. pendiente de facturar = todo el certificado. La estructura queda lista para cuando llegue el dato.

### Parte 6 — Ajustes finos de "Avance y Certificaciones"
- **Avance por etapa:** + columnas **Cert. B / Cert. N** (apertura fiscal del certificado de avance por
  presupuesto; B+N = Cert. avance, verificado exacto en las 3 obras). Reader: `cert.py` arma `oc_bn`
  (B/N por OC desde `Cert_Calculo`, sin ANT) → `avance_etapa.items[].cert_bn` + `total.cert_bn`.
- **Certificaciones recientes → "Última certificación por OC":** muestra sólo la cert más reciente de
  cada OC (excluye anticipos), con leyenda explicativa. Frontend (`ds.js`).
- **Apertura fiscal:** card eliminada (la info B/N ahora vive en Avance por etapa y en el drawer).
- **Detalle de certificados:** agrupado/ordenado por OC (sort `(concepto, fecha, id)` en `_ledger`).
- **Fechas de cert arregladas:** GDR/SVD guardaban la fecha de cert como **serial de Excel** (45747)
  con number_format General → openpyxl no la convertía. Fix en `cert.py::_to_iso` (usa
  `openpyxl.utils.datetime.from_excel` para serials). Verificado: 0 fechas sin parsear en las 3.
- **Drawer de certificación enriquecido (punto 6):** tabla **Desglose por tipo fiscal** (Blanco /
  Negro / Total) con Importe base · +CAC · +IVA · =Total cert · Monto USD (U$, sólo Negro) ·
  Facturado · Sin facturar · Cobrado. Reader: `_ledger` arma `partes` por cert (B/N con
  base/cac/iva/usd/total/facturado/cobrado) + `facturado`/`sin_facturar`/`usd` por cert;
  `_facturacion` ahora **agrega por idfact** (1:N comprobantes: Σ monto, Σ cobrado).
- Cambios del reader todos **aditivos**. Flask 200 en las 3; screenshots OK.

### Parte 7 — Avance por etapa simplificado + drills enriquecidos
- **Tabla "Avance por etapa"** reducida a: OC · Descripción · PPTO Venta · **Anticipo + Cert. Avance**
  (= certificado total de la OC, una columna) · **Avance** (barra + %, = avance físico col E). Se
  quitaron las columnas Cert. B/N (van al drill). Decidido con Pedro (avance físico; columna única).
- **Drill de OC** (`openEtapaDrawer` ahora recibe el objeto etapa): recap arriba con PPTO Venta +
  **Anticipo y Certificado de avance por B/N ($ y %)** + **CAC** e **IVA** totales; debajo, el avance
  por tarea (como estaba). Reader: `cert.py::_ledger` arma `oc_res` por OC (ant_b/n, cert_b/n, cac,
  iva) → `avance_etapa.items[].{anticipo_bn, cert_bn, cac, iva}`.
- **Drill de certificado** (`openCertDrawer` ahora async + recibe `code`): al desglose por tipo fiscal
  se agregaron filas **% facturación** y **% IVA**, y una línea de parámetros con **índice de
  actualización CAC** y **TC USD (MEP)**. Debajo, **tabla nueva de tareas que compusieron el
  certificado** (Cód · Tarea · % actual · % acum · $ base). Reader: `partes` ahora trae `pct_fact`/
  `pct_iva`; item trae `cac_ratio`/`tc_usd`. **Endpoint nuevo** `GET /api/obras/<code>/cert?id=` →
  `drilldown.cert_detail` (lee `Cert_App_Output` por ID Certif, descripción desde `1_Presupuesto`).
- Verificado: endpoint 200 + cert_detail 12 tareas/$32,7M (GDR-OC01-C01); recap OC con B/N+CAC+IVA;
  screenshots de tabla, drill OC y drill cert OK. Flask 200 en las 3 obras.

### Parte 8 — Drill de OC: matriz fiscal aditiva (responde "¿por qué cert > ppto?")
Consulta de Pedro: "Anticipo + Cert. Avance" ($618,8M GDR-OC01) > PPTO Venta ($500,9M). **Causa: el
certificado incluye CAC + IVA; el PPTO Venta es la base sin ajustes.** Verificado con datos: base
certificada $500,9M = PPTO Venta EXACTO; el exceso = CAC $38,6M + IVA $79,3M = $117,9M.
- **Reader** (`cert.py`): `oc_res` reestructurado a descomposición ADITIVA por tipo fiscal —
  `fiscal.{anticipo, avance, cac, iva, total}` con `{blanco, negro}` cada uno (base neta col L para
  anticipo/avance; CAC col P; IVA col S). Los 4 componentes suman exacto al total certificado
  (verificado CH/GDR/SVD). + `n_certs` por OC (certs no-ANT).
- **Drill de OC** (`openEtapaDrawer`): figs **PPTO Venta · Total certificado · % avance · N° certif.**;
  matriz **Componente × (Blanco/Negro/Total)** con filas Anticipo (base), Certif. avance (base),
  +Ajuste CAC, +IVA, y fila **TOTAL** (totales de fila y columna). Mismo formato de número en las 3
  columnas; filas de ajuste CAC/IVA en **itálica/atenuado** (clase `.fiadj`) para diferenciarlas de la
  base; TOTAL en negrita. Nota: "la base es comparable al PPTO Venta; CAC+IVA son los ajustes".

### Parte 9 — Tabla Avance por etapa con descomposición + ajustes al drill
- **Tabla "Avance por etapa"** (full-width ahora): OC · Descripción · Ppto Venta · **Avance** (barra+%) ·
  **Monto Base Cert.** (neto desacopio) · **CAC** · **IVA** · **Cert. Avance** (= base+CAC+IVA = total
  certificado, incluye anticipo sin nombrarlo). Hace visible en la tabla por qué el certificado supera
  el ppto. Layout: avance full-width arriba; debajo grid [Detalle de certificados | Última cert. por OC].
- **Drill de OC:** título → "Composición fiscal de **la certificación**"; quitado "(a precio ppto)" de
  las filas y la nota; **+ columna Cód** en la tabla de partidas (reader `etapa_detail` ahora trae `cod`).
- Flask 200 en las 3; screenshots OK.

### Pendiente para próxima sesión / datos
- Ver `pendientes.md` (typo "Homigón" en CH, mapeo Edenor/Aysa, Payroll Socios sin línea de ppto).
- Manual `.docx` (`Resumen_de_Obra_Manual_de_Uso_v8.docx`) no actualizado (es binario; el HANDOFF .md sí).
