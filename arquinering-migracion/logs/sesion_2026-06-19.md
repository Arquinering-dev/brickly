## Sesión 2026-06-19

### Objetivo
Pedro envió el **Plan de Cuentas de Tezamat** (`archivos/fuente/AING - Plan de Cuentas.xlsx`),
desbloqueando el pendiente "plan de cuentas = fuente de verdad de rubros". Reconciliar `_Listas`
de CH contra el plan y cablear dropdowns + sanity checks (decisión 2026-06-18).

### Completado
- [x] **Inspección del plan**: plan contable completo (292 cuentas). Rama **53 OBRA** (31 imputables)
      = rubros de obra con split MT/MO en el nombre; ramas **50/51/52** = indirectos; **41/42** = ingresos.
- [x] **Reconciliación** (`scripts/reconciliar_plan_cuentas.py` → `docs/Reconciliacion_Plan_Cuentas_CH2171.md`):
      las **15 cuentas que CH usa en `2_Movimientos` resuelven TODAS contra el plan por código**.
      Cruce posible por código (col A), no por texto. 7 rubros de `1_Presupuesto` sin match exacto.
- [x] **Decisiones tomadas con Pedro** (ver abajo).
- [x] **Aplicación** (`scripts/aplicar_plan_cuentas_ch.py`) → `CH_2171_..._v8_7.xlsx`
      (backup `_bak_CH_pre_plancuentas.xlsx`):
      - `_Listas` reescrito = rama 53 OBRA (con código) + Mov.Variables (52302) + Supervisión (52209).
      - Renames/merges en `1_Presupuesto` (10 celdas): Agrimensura→Preliminares; GG Obra MT/MO +
        Mantenimiento→Gastos Generales; Seguridad e Higiene MO→Seguridad e Higiene.
      - Dropdowns (DV con **nombre definido `RUBROS_PLAN`**) en `1_Presupuesto` A:D y `2_Quincenas` E.
      - Sanity `_Listas!F2/F3` (rubros de tarea ∈ plan = ✓✓).
- [x] **Verificación**: `recalc.py` 0 errores; **total presupuestado preservado exacto**
      (1.109.935.042,56, Δ=0,00); diff del control v2: 6 rubros fusionados desaparecen, 0 espurios;
      cruce presup↔gasto mejora (Gastos Generales y Seguridad e Higiene dejan de ser sin-ppto falsos;
      sin-ppto 8→6). Reader v2 completo OK (8 módulos).
- [x] **Deploy**: v8_7 en `archivos/output` + `dashboard_v2/data`; `config/obras.yaml`→v8_7;
      entrada en CHANGELOG del libro. Memoria `plan-cuentas-tezamat-rubros`.

### Decisiones tomadas
- **Política de nombres**: alinear nombres de rubro v8 EXACTAMENTE al plan (incluido typo `Homigón MO`).
- **Gastos Generales**: colapsar `Gastos Generales Obra MT/MO` → único `Gastos Generales` (53022).
- **Mapeos caso por caso**: Agrimensura MT/MO→Preliminares (53001); Supervisión de Obra MO→H. Ingeniería
  (52209, indirecto); Mantenimiento MO→Gastos Generales (53022).
- **Mov. Variables**: se mantiene como rubro de obra (mapea a 52302).
- **Supervisión de Obra**: por ahora queda como rubro marcado indirecto; el MOVER su presupuesto a la
  sección de indirectos es paso siguiente separado.
- **DV cross-sheet**: usar **nombre definido**, no `=_Listas!...` directo (Excel lo guarda como x14 y
  openpyxl lo descarta).

### Pendiente para próxima sesión
- [ ] Mover presupuesto de Supervisión de Obra (~$61,7M) a la sección de indirectos.
- [ ] Enrutar cuentas 50/51/52 (Aguas, Gastos en personal, H. Gestoria, H. Seg e Higiene) a la sección
      de indirectos en el reader de dashboard_v2 (hoy aparecen como rubros sin-ppto).
- [ ] Definir si Herrería MO / Varios Ferreteria (53, gasto sin línea de ppto) llevan presupuesto.
- [ ] Aplicar el mismo alineamiento de plan a GDR cuando se acondicione a v8_6/v8_7.

### Errores encontrados y resueltos
- DV cross-sheet `=_Listas!$A$2:$A$34` → Excel la convirtió a extensión x14 (openpyxl la descarta al
  leer). Resuelto usando nombre definido `RUBROS_PLAN` (DV estándar, sobrevive el round-trip COM).

---

## Sesión 2026-06-19 (parte 2) — Reorganización del proyecto + reencuadre de CLAUDE.md

### Objetivo
A partir de `docs/HANDOFF_flujo_resumen_v8.md` §7 (el proyecto mutó de *migración a v8* a
*ecosistema Excel-DB + dashboard web + app de certificación*): limpiar la carpeta archivando lo
superado (sin borrar) y actualizar el `CLAUDE.md` al alcance nuevo. Workflow con checkpoint:
propuesta → confirmación de Pedro → ejecución.

### Completado
- [x] **Propuesta de archivado** revisada con Pedro (qué conservar vs mover a `_archivo/`).
- [x] **Decisiones de Pedro**: (1) jubilar dashboard v1, migrando a v2 lo útil; (2) conservar
      `migracion_egresos_v3/4/5.py` en `scripts/`; (3) `_archivo/` dentro del repo + gitignored.
- [x] **Migrado de `dashboard/` v1 → `dashboard_v2/`**: `LOGICA_CALCULO_v8.md` (resultó ser del
      paradigma nuevo: contrato del motor que recalcula), `specs/ESPEC_Circuito_Certificacion_v8.md`,
      `design/` (DS Industrial Integrity + mockups), `CLAUDE.md` (con nota de qué de ahí ya no aplica).
- [x] **Archivado en `_archivo/`** (115 archivos, espeja carpetas, con `_archivo/README.md`):
      dashboard v1 completo · 10 versiones/backups CH de output (v8_1..v8_5 + 4 `_bak_CH_*`) ·
      v8_6 + 5 `_bak_GDR_*` de referencia · v8_6 de data v2 · 17 scripts `_*` · 3 docs viejos ·
      4 logs de debug/plantilla.
- [x] **Borrado (basura regenerable)**: `__pycache__`/`*.pyc`, `*_srv.log`, lock `~$` de Excel.
- [x] **`.gitignore`**: agregado `_archivo/`.
- [x] **`CLAUDE.md` raíz reescrito**: título + nota de paradigma; §1 con conceptos nuevos (Excel=DB,
      Tezamat, Plan de Cuentas=rubros, regla de los 4 rubros) y tabla de obras actualizada (CH v8_7
      vigente); §2 Objetivo = las 3 capas del ecosistema; §6 árbol y roles a la estructura real;
      §11 con 2 decisiones nuevas (plan de cuentas v8_7 + esta reorganización).
- [x] **Verificado**: `dashboard_v2/config/obras.yaml` apunta a CH v8_7 (existe en `data/`);
      sin referencias obsoletas residuales en `CLAUDE.md`.

### Estado del árbol vivo (post-limpieza)
`CLAUDE.md`, `README.md`, `archivos/` (fuente · referencia=GDR v8+APUs · output=CH v8_7),
`dashboard_v2/`, `scripts/` (validadores + plan ctas + migracion_egresos v3/4/5), `docs/`,
`logs/`, `memory/`, `_archivo/`.

### Decisiones tomadas
- Dashboard v1 **jubilado** (no se mantienen dos dashboards): v2 recalcula desde fuente cruda,
  que es el paradigma rector. Lo reutilizable del v1 vive ahora en `dashboard_v2/`.
- `_archivo/` es **solo consulta**; no reactivar material sin aviso.

---

## Sesión 2026-06-19 (parte 3) — Addendum circuito Cert_* (HANDOFF §5/§10) → v8_8

### Objetivo
Aplicar las decisiones nuevas del handoff al circuito de certificación/ingresos (el v8_7 se había
construido sobre el ESPEC viejo de v8_1). **Solo forma/estructura, value-preserving; NO conciliación
contra `2_Movimientos`** (eso es el spec siguiente). Script: `scripts/addendum_cert_ingresos.py`.
Entrada v8_7 → salida **`CH_2171_Resumen_de_Obra_v8_8.xlsx`** (v8_7 queda backup).

### Completado (4 puntos confirmados por Pedro)
- [x] **Desacopio a nivel madre**: hoja nueva **`Cert_Cabecera`** (1 fila por certificación madre:
      ID Certif, ID OC, Fecha, $ base bruta, **% desacopio único (input)**, $ desacopio, subtotal neto,
      chequeo). `Cert_Calculo!J` pasó de **input azul → referencia verde** (`XLOOKUP` a la cabecera).
      Value-preserving: el % por madre = el que ya tenía por rama (0,2 / 0,4 / 0).
- [x] **CAC con override**: nueva col input `Cert_Calculo!Y` "Indice CAC override" (vacía por defecto);
      `N` pasó a `=IF($Y="", XLOOKUP(...), $Y)` — fórmula por defecto + override manual (patrón tipo MEP).
      Override sobre el **índice a la fecha** (decisión de Pedro), ratio `O=N/M` sin cambio.
- [x] **id_factura estructurado**: formato `{id_cert_fact}-F{NN}` (ej. `CH-OC01-C01-B-F01`), colgando
      del Blanco, admite >1 factura por certificación. El nº humano/AFIP queda en `Comprobante` aparte.
- [x] **`Cert_Facturacion` forma final (un solo toque)**: `monto` (moneda del cobro) + `moneda` (ARS/USD)
      + `monto_ars_equiv` (=`IF(USD, monto×TC_cobro, monto)`) + nuevas `id_factura`, `TC cobro`,
      `id_cert_madre`, `id_OC` (columnas propias derivadas) y `Clase` (Certificación/Anticipo). El flag
      manual `Conciliado` se dejó intacto (fuera de alcance).
- [x] **Consecuencia**: `Cert_Control_OC!G/H` re-apuntados de `Facturacion!D` → `monto_ars_equiv`.

### Decisiones tomadas
- **Anticipo no es un `Tipo` plano**: las filas `-ANT` están partidas Blanco/Negro igual que una
  certificación. Se mantuvo `Tipo`=Blanco/Negro y se agregó columna derivada **`Clase`** (vía
  `id_cert_madre` LIKE `*ANT*`) para distinguir el anticipo sin perder la partición fiscal.
- **TC del cobro de ejemplo** = se derivó `TC = D_old/E_old` por fila Negro para **preservar
  exacto** `monto_ars_equiv = D_old` (Δ<1e-6 en las 7 filas). Estructura lista para que el TC real
  difiera del MEP de la certificación.

### Verificación
- `recalc.py` sobre v8_8: **0 errores** (6.768 fórmulas).
- Chequeo estructural + preservación de `monto_ars_equiv` en las 7 filas Negro: **OK exacto**.
- ⚠ `recalc.py` no evalúa fórmulas: la validación final real (XLOOKUP/etc.) y el **cacheo de
  valores** para el dashboard requieren **abrir v8_8 en Excel y guardar** una vez (como todo cambio
  de Cert_*).

### Pendiente para próxima sesión
- [ ] **Spec de conciliación Tezamat** (HANDOFF §10, punto 6): cruce `Cert_Facturacion`↔`2_Movimientos`,
      doble registro base/CAC, strings a copiar en observaciones, y reemplazo del flag `Conciliado`.
- [ ] **Dashboard `dashboard_v2/reader/cert.py`**: lee el layout viejo de `Cert_Facturacion`
      (`$ Monto`/`USD Monto`). **NO repuntar el dashboard a v8_8** hasta actualizar el reader al
      layout nuevo (monto/moneda/monto_ars_equiv). Config sigue en v8_7.
- [ ] Abrir v8_8 en Excel y guardar (cacheo + validación real).
- [ ] Heredan los pendientes de las partes 1-2 (Supervisión de Obra a indirectos, enrutar 50/51/52,
      Herrería MO/Varios Ferretería, alinear el plan a GDR).

---

## Sesión 2026-06-19 (parte 4) — Conciliación contra 2_Movimientos → v8_9

### Objetivo
Ejecutar `docs/SPEC_Conciliacion_Movimientos_v8.md`: ligar `2_Movimientos` (extracto Tezamat) con las
dos puntas — ingresos (certificaciones) y egresos (subcontratos) — con estado **computado** (no flag
manual), respetando la regla CAC/CS (no descuentan saldo). Script: `scripts/spec_conciliacion_movimientos.py`.
Entrada v8_8 → salida **`CH_2171_Resumen_de_Obra_v8_9.xlsx`** (v8_8 backup).

### Completado (4 decisiones confirmadas por Pedro)
- [x] **Convención de Observaciones** `{ID} | {TIPO} | {desc}` (TIPO ∈ BASE/CAC/ANT/CS). Se aplica
      sobre `2_Movimientos!E` (editable); `P` = "Observaciones original" (respaldo crudo Tezamat, intacto).
- [x] **Normalización** de las **12 filas SC** de `E` al convenio (`AVANCE`→`BASE`, `CAC`→`CAC`, desc de
      `P`). Las **11 `QUINCENA`** (nómina propia, sin ID de SC) se dejaron como están → `mov_id` vacío,
      no concilian (correcto).
- [x] **Parseo** en `2_Movimientos`: cols derivadas `Q mov_id` / `R mov_tipo` (FIND/MID/UPPER, compat 2016).
- [x] **Egresos** (`2_Subcontratos`, cols H-N): `Pagado BASE+ANT` (descuenta) · `CAC pagado` · `CS pagado`
      (no descuentan) · `Total pagado` · `Saldo disponible` · `% consumido` · `Estado` (semáforo).
- [x] **Ingresos** (`Cert_Facturacion`): + `Retención` (P, input) + `Haber conciliado` (Q, SUMIFS BASE
      por `id_cert_fact`); **`H` repurposado de flag manual → `Estado conciliado` computado**
      (`Conciliado` si `Haber+Retención=certificado ± tol`; si no `Parcial`/`Pendiente`). Concilia `-B` y
      `-N` (los cobros USD Negro entran a `2_Movimientos` como Haber — decisión de Pedro).
- [x] **Tolerancia** = parámetro `0_CONFIG!B49` (sugerido **$100** absoluto), referenciado por el match.

### Verificación
- `recalc.py` v8_9: **0 errores** (7.076 fórmulas).
- **Egresos concilian sobre datos reales** (verificación numérica del script): SC-001/004 saldo 0 ·
  **SC-002 sobrepagado −$50.000** (730k vs 680k presup) → lo surfacea el control · SC-003 saldo $5,18M con
  CAC $1,14M **correctamente excluido** del saldo (regla CAC/CS OK).
- **Ingresos: estructura lista pero inerte** sobre datos actuales — no hay ningún movimiento de Haber
  tagueado con ID de certificación (los ingresos hoy son depósitos lump del Fideicomiso, cta 410222).
  El estado da `Pendiente`/`—` hasta que Tezamat cargue cobros por-cert con la convención.

### Decisiones / hallazgos
- **Quincenas no son CS-de-SC**: son nómina propia sin ID de subcontrato → fuera del cruce SC.
- **SC-002 sobrepagado** ($50k): registrar con Arquinering (¿error de carga o pago extra pactado?).
- Estado ingresos compara contra `$ certificado (control)` total (Cert_Calculo!T); refinable a base/CAC
  por separado cuando lleguen cobros tagueados (hoy sin efecto observable porque no hay match).

### Pendiente para próxima sesión
- [ ] **Actualizar `dashboard_v2/reader/cert.py` UNA sola vez** contra el layout final (addendum v8_8 +
      conciliación v8_9): `Cert_Facturacion` ahora `monto`/`moneda`/`monto_ars_equiv`/…/`Retención`/
      `Haber conciliado`/`Estado conciliado`; y leer `2_Subcontratos` cols H-N + `2_Movimientos` mov_id/tipo.
      Recién entonces repuntar el dashboard de v8_7 → v8_9.
- [ ] **Tezamat**: cargar cobros de cliente por-cert con la convención `{cert-id}-B | BASE | …` (+ `-N`,
      `CAC`) para activar la conciliación de ingresos. Idem pagos SC en próximos volcados.
- [x] ~~Abrir v8_9 en Excel y guardar (cacheo + validación real)~~ **HECHO** (ver parte 5).
- [ ] Revisar SC-002 sobrepago con Arquinering.

---

## Sesión 2026-06-19 (parte 5) — Validación real en Excel de v8_9

### Objetivo
Validar de verdad las fórmulas acumuladas de los 3 specs (migración + addendum v8_8 + conciliación v8_9):
`recalc.py` no evalúa fórmulas, solo detecta errores cacheados. Se forzó recálculo completo por COM.

### Completado
- [x] **`scripts/excel_recalc.py`** sobre v8_9 (Excel COM, `CalculateFullRebuild` + Save): repobló los
      valores cacheados. v8_9 incluye TODO lo de v8_8, así que valida ambos specs de una.
- [x] **`recalc.py` post-recálculo: 0 errores** (55.091 celdas / 7.076 fórmulas) + scan `data_only`:
      **0 errores cacheados** en todo el libro. Ninguna XLOOKUP/MAXIFS/SUMIFS rompe.
- [x] **Spot-check de valores** (data_only): Cert_Cabecera subtotal neto OK (C01: base 197,5M − desac
      39,5M = 158M); Cert_Calculo desacopio/CAC/ratio OK; Cert_Facturacion `id_cert_madre`/`Clase`/Estado
      OK (`🔴 Pendiente`, Haber=0 → ingresos inertes, correcto); `mov_id`/`mov_tipo` parsean bien;
      2_Subcontratos saldos exactos (SC-002 −50k, SC-003 5,18M con CAC excluido).

### Hallazgo menor (refinamiento opcional)
- El semáforo de `2_Subcontratos!Estado` da `🔴 Sin saldo` tanto para contrato **completado** (saldo=0,
  SC-001/004) como **sobrepagado** (saldo<0, SC-002). Convendría distinguirlos (p.ej. `🟢 Cerrado` si
  saldo=0, `🔴 Sobregiro` si saldo<0) — idealmente cruzando con un flag de "tarea terminada" / % avance.
  No es bug; queda como mejora.

### Estado
v8_9 **validado y con valores cacheados** → listo para que el dashboard lo consuma una vez actualizado
el reader. La deuda de validación de los 3 specs queda **saldada**.

---

## Sesión 2026-06-19 (parte 6) — Carga de certificaciones REALES OC01 → v8_10

### Objetivo
Pedro envió el documento real `archivos/fuente/CH 2171_Presupuesto 01_Cert. #2.xlsx` (certs #1 y #2 de OC01).
Cargar el avance físico real, vaciar lo inventado, y marcar lo faltante. Script: `scripts/cargar_cert_reales_oc01.py`.
Entrada v8_9 → salida **`CH_2171_Resumen_de_Obra_v8_10.xlsx`** (v8_9 backup).

### Completado
- [x] **Cruce y verificación**: el avance real reconcilia con el documento por clave compuesta PTO 01
      (Cert#1 $64.007.081 vs doc $64.007.157 Δ−$76; Cert#2 $68.905.428 vs $68.905.452 Δ−$24; subtotal
      neto $55.124.342 vs $55.124.362; **avance físico OC01 = 22,13% exacto**). Δ = redondeo de P unit.
- [x] **`Cert_App_Output`**: vaciado y recargado con **32 filas reales** (C01: 14 tareas, C02: 18), key
      PTO 01. `% anterior/actual/total` del documento.
- [x] **Vaciado de lo inventado**: OC01 C03/C04 y todo OC02 en `Cert_Calculo`/`Cert_Cabecera`; **todos los
      cobros de `Cert_Facturacion`**. (Sin `delete_rows`: celdas a None.)
- [x] **MEP real**: `Cert_Calculo!U` de las certs conservadas → $1.276 (DÓLAR MEP del documento).
- [x] **Verificación**: `recalc.py` 0 errores + **recálculo real Excel** (excel_recalc) → **0 errores
      cacheados**; `1_Presupuesto!AC` por tarea = acumulado del documento.
- [x] **Pendientes** (`pendientes.md`): OC02 sin certs, partición fiscal provisional (defaults OC), cobros
      reales faltantes, y **el detalle de observaciones de Tezamat para los ingresos de OC01** (strings
      `CH-OC01-{ANT|C01|C02}-{B|N} | {ANT|BASE|CAC} | …` + acción de separar los depósitos lump del Fideicomiso).

### Decisiones
- **OC02 y cobros**: vaciados (no hay datos reales) → quedan como placeholder a la espera de sus documentos.
  El contrato OC02 (`Cert_OC_Cliente`) se conserva (es real); solo se vaciaron sus certificaciones.
- **Fiscal**: split Blanco/Negro/IVA queda en defaults de la OC (provisional). Los KPIs reales son base
  certificado / subtotal neto / avance físico; el "$ Certificado total" con CAC+IVA es provisional.

### Pendiente
- [ ] Documento de avance de **OC02** para cargarlo igual que OC01.
- [ ] **Partición fiscal real** + **cobros reales** (anticipo + cert #1/#2) → cargar `Cert_Facturacion`.
- [ ] **Tezamat**: taguear los cobros del Fideicomiso con las observaciones detalladas en `pendientes.md`.
- [x] ~~(Track 2) actualizar el reader del dashboard y repuntar a v8_10~~ **HECHO** (ver parte 7).

---

## Sesión 2026-06-19 (parte 7) — Track 2: dashboard al layout final, repunte a v8_10

### Objetivo
Actualizar el reader de `dashboard_v2/` a los cambios acumulados (addendum v8_8 + conciliación v8_9 +
datos reales v8_10) y repuntar el dashboard de v8_7 → v8_10.

### Completado
- [x] **`reader/movimientos.py`** — `_parse_sc` ahora parsea la convención `{ID} | {TIPO} | {desc}`
      (tipo = token 2: BASE/CAC/ANT/CS), con fallback legacy. Antes buscaba `AVANCE` suelto (roto por la
      normalización).
- [x] **`reader/read_obra.py`** — `_subcontratos`: matriz descuenta-saldo a la regla nueva (BASE+ANT
      descuentan; CAC y CS no).
- [x] **`reader/cert.py`** — `_facturacion`: lee `monto_ars_equiv` (no la col multimoneda), `Retención`,
      y deriva conciliado del `Estado conciliado` computado (reemplaza el flag manual). Retención conectada
      al ledger (antes hardcodeada en 0).
- [x] **Repunte**: copiado v8_10 a `dashboard_v2/data/` (quitado v8_7); `config/obras.yaml` → v8_10.
- [x] **Validación end-to-end**: reader parsea sin excepciones; Flask app responde
      `/api/obras`, `/api/obras/CH`, drill `subcontrato`, y vistas `/` · `/ds` · `/ds/obra/CH` → **HTTP 200**.
      KPIs coherentes: avance PTO 01 22,13% (= documento), avance obra 8,63% (ponderado 2 OC), subcontratos
      con regla CAC/CS (SC-002 rojo −50k, SC-003 CAC excluido), 3 cert items, semáforo amarillo.

### Notas
- **Avance físico obra = 8,63%** es correcto (ponderado: PTO 01 22,13% + PTO 02 0%, por valor de contrato);
  el 22,13% del documento es PTO 01, visible en avance por etapa.
- `drilldown.subcontrato_detail`: el split base/cac trata solo CAC como no-descuenta; CS iría al bucket base
  (no hay datos CS de SC hoy). Refinamiento menor pendiente si aparece CS de subcontrato.
- Cobros/ingresos siguen inertes (Cert_Facturacion vacía) hasta cargar cobros reales + taguear Tezamat.

### Pendiente
- [ ] Documento de avance de OC02 + cobros reales + partición fiscal (heredado de parte 6).
- [ ] Tezamat: taguear cobros del Fideicomiso (observaciones de `pendientes.md`).
- [ ] GDR al estándar v8_x (track 4) para multi-obra real.

---

## Sesión 2026-06-19 (parte 8) — Control Presupuesto por bloques (rama plan de cuentas)

### Objetivo
Separar el Control de Presupuesto por bloque de cuenta (buena práctica): **Costo de Obra** (directo
controlable) / **Indirectos / Estructura** / **Financieros / Impuestos**, en vez de una lista plana donde los
indirectos aparecían como "rubros sin presupuesto". Solo cambio en el dashboard (reader + frontend), no en el Excel.

### Decisiones de Pedro
- **3 bloques**: Obra (53 + Mov. Variables, que tiene ppto), Indirectos (51/52), Financieros (50). Ingresos (4x) fuera.
- **Presupuesto de indirectos/financieros = `1_GGBB` columna G** ("por fuera de obra", aplicado al precio de
  venta vía coeficiente). Los ítems con J/K están **embebidos en el presupuesto de obra** (`1_Presupuesto`) y
  NO se cuentan acá. Financieros = IIBB + Imp. cheque ($58,4M); Indirectos = resto de G ($168,6M).
- **Aguas** está mal clasificada (es *Gastos de oficina*) → pendiente de reclasificar en Tezamat; queda en Indirectos.

### Completado
- [x] **`reader/read_obra.py`**: `_ggbb_presupuesto` (lee col G, acotado a "Total de Gastos Generales",
      financieros por keyword IIBB/Imp.cheque); `_control_ppto` refactorizado en bloques con subtotales;
      `_bloque()` clasifica por rama del código (`2_Movimientos!A`). Contrato: `control_ppto.bloques` +
      campo `bloque` por rubro; `total` = bloque Obra (controlable); `top_desvios` = solo obra.
- [x] **`web/js/ds.js`**: módulo Control reescrito en 3 cards de bloque (Obra con tabla completa + drill;
      Indirectos/Financieros con presupuesto GGBB a nivel de bloque + gasto por cuenta). Card del dashboard
      filtrada a rubros de obra.
- [x] **Validado end-to-end**: API devuelve los 3 bloques (Obra $1.109,9M/6,6%, Indirectos $168,6M/2,2%,
      Financieros $58,4M/0%); `/ds/obra/CH/control` HTTP 200; **screenshot headless confirma el render**;
      drill de rubro sigue OK; `ppto_costo` resumen intacto ($1.109,9M).

### Pendiente
- [ ] Reclasificar Aguas → Gastos de oficina en Tezamat (ver `pendientes.md`).
- [ ] Definir si los rubros de obra gasto-only (Seguridad e Higiene, Herrería MO, Termomecánica) llevan ppto.
- [ ] (Opcional) mapeo GGBB↔cuenta si se quiere presupuesto de indirectos por cuenta (hoy a nivel de bloque).
- [ ] GDR al estándar v8_x (track 4).

---

## Sesión 2026-06-19 (parte 9) — Control Presupuesto: 4 bloques + orden de etapa + UI prolija

### Objetivo (pedido de Pedro)
Reestructurar el Control: (1) primer bloque = rubros de obra **ordenados por etapa** (orden del presupuesto:
Preliminares → Mov. de Suelos → Hormigón → …); (2) tres bloques GGBB — **Gastos Generales / Directos /
Indirectos** — presupuestados por `1_GGBB` **columna G** (no los ítems J/K que están en el ppto de obra);
(3) UI más prolija (rubros en una sola fila, menos compacto).

### Completado
- [x] **`reader/read_obra.py`**: `_presupuesto` ahora devuelve `orden` (primera fila por rubro = orden de
      etapa); `_ggbb_secciones` reemplaza a `_ggbb_presupuesto` (parsea las 3 secciones de GGBB por header,
      suma col G por ítem, acota a "Total de Gastos Generales", excluye Equipos); `_control_ppto` arma 4
      bloques: Obra (rubros ordenados por etapa) + Generales/Directos/Indirectos (ítems GGBB + gasto de
      cuentas Tezamat asignadas por `_seccion_de_cuenta`). IIBB/Imp.cheque quedan en Gastos Indirectos
      (se eliminó el bloque "Financieros" separado).
- [x] **`web/js/ds.js`**: módulo Control con card Obra (tabla completa + drill, orden de etapa) + 3 cards GGBB
      (`ggbbCard`: tabla de ítems presupuestados + "GASTO INCURRIDO (TEZAMAT)" con las cuentas + consumo del bloque).
- [x] **`web/css/ds.css`**: tablas menos compactas (alto 32→38px, padding↑) y `td:first-child{white-space:nowrap}`
      (rubros en una sola fila).
- [x] **Validado**: API 4 bloques (Obra $1.109,9M/6,6% · Generales $42,9M/7% · Directos $89,9M/0,4% ·
      Indirectos $94,2M/0,5%); `/control` HTTP 200; **screenshot headless confirma render prolijo y orden de etapa**.

### Notas / pendiente
- **Mapeo gasto→sección GGBB es best-effort** (keyword): Aguas/H.Seg→Generales, Gastos en personal→Directos,
  H.Gestoría→Indirectos. **Confirmar con Pedro** (sobre todo Gastos en personal). Ver `pendientes.md`.
- Sigue pendiente reclasificar Aguas→Gastos de oficina en Tezamat.

---

## Sesión 2026-06-19 (parte 10) — 2_Gastos_DirInd a tabla plana → v8_11

### Objetivo
Reestructurar `2_Gastos_DirInd` de **matriz (concepto × mes)** a **tabla plana** tipo `2_Movimientos`, para
que la carga sea más fácil (append de filas). La hoja es solo almacén; el diseño/agrupado va en el dashboard.
Script: `scripts/reestructurar_gastos_dirind.py`. Entrada v8_10 → salida **v8_11** (v8_10 backup).

### Decisiones de Pedro
- **4 columnas**: `Fecha | Tipo | Concepto | Monto` (sin Proveedor/Comprobante/Observaciones — "son siempre
  los mismos conceptos").
- `Tipo` = Directo/Indirecto (alinear a secciones GGBB más adelante, con la data limpia).
- Versión nueva v8_11 + repuntar dashboard.

### Completado
- [x] Matriz → tabla plana: **13 registros migrados value-preserving** (Proyecto/Coordinación/Vehículo en
      Directos; Payroll Socios/Administración en Indirectos; mar/abr/may 2026). Se excluyeron las columnas
      "Total Real"/"Total Descontado" (las deriva el dashboard) y se descartaron las filas de Ratio CAC,
      "Estado del mes" y totales.
- [x] **Dropdowns** (carga más fácil): `Tipo` inline (Directo/Indirecto) + `Concepto` vía named range
      `GASTOS_CONCEPTO` (lista de 16 conceptos en `_Listas` col I).
- [x] **Verificado**: recalc 0 errores; **excel_recalc OK y los DV + named range sobreviven el round-trip COM**
      (B2:B500 Tipo, C2:C500 Concepto). Dashboard repuntado a v8_11 (config + data); `/api/obras/CH`,
      `/ds/obra/CH`, `/control` → HTTP 200 (el reader no lee esta hoja, output idéntico).

### Pendiente
- [x] ~~**Cablear `2_Gastos_DirInd` al dashboard**~~ **HECHO** (parte 11): conectado al Cash Flow.
- [ ] Alinear `Tipo` a secciones GGBB (Generales/Directos/Indirectos) cuando se definan los bloques con data limpia.
- [ ] (Opcional) sumar los gastos dir/ind al Control Presupuesto (bloques GGBB) además del Cash Flow — pendiente
      de la limpieza de data y la definición de bloques.

---

## Sesión 2026-06-19 (parte 11) — Conectar 2_Gastos_DirInd al Cash Flow del dashboard

### Objetivo
Conectar la tabla plana `2_Gastos_DirInd` (v8_11) al dashboard. Los placeholders del Cash Flow
(`gastos_directos`/`gastos_indirectos` = 0) ahora se llenan desde la hoja.

### Completado
- [x] **`reader/movimientos.py`**: `load_gastos_dirind(wb)` lee la tabla plana → `{fecha, mes, tipo,
      concepto, real, descontado}` (descontado = real × ratio CAC del mes, como `2_Movimientos`).
- [x] **`reader/read_obra.py`**: `_cash_flow(movs, gdi)` llena las series `gastos_directos`/`gastos_indirectos`
      por mes y los suma a `total_egresos`; `_parse` pasa `gdi`. → el resultado_mes/acumulado, valle y KPIs
      del Cash Flow reflejan los dir/ind.
- [x] **`reader/drilldown.py`**: `mes_detail` agrega los gastos dir/ind del mes como filas "Gastos Directos"
      / "Gastos Indirectos" (para que el drill reconcilie con el total del mes).
- [x] **Validado**: cash flow series llenas (Σ directos $4,85M, indirectos $5,45M); `/ds/obra/CH/cash` HTTP 200;
      **screenshot confirma** egresos/mes con dir/ind incluidos, chart y KPIs actualizados. El frontend no
      necesitó cambios (usa `total_egresos`).

### Notas
- El drill de mes da Δ ~$45.8k vs el total del cash flow en meses con un movimiento de Haber en cuenta de
  egreso (el cash netea el negativo, el drill lo omite). **Quirk pre-existente**, no introducido por este
  cambio (los dir/ind se suman consistente a ambos lados).
- Los gastos dir/ind van **solo al Cash Flow** por ahora; sumarlos al Control Presupuesto (bloques GGBB)
  queda para cuando se limpie la data y se definan los bloques.

---

## Sesión 2026-06-19 (parte 12) — Auditoría integral + separación del anticipo

### Auditoría (diagnóstico, sin cambios)
Informe completo en el chat. Técnico **sano**: recalc 0 errores, 0 errores cacheados, 0 refs a hojas
eliminadas, 0 rangos SUMIFS cortos, 0 valores pegados, IDs Cert_* consistentes, rubros 100% en plan, CAC al día.
Hallazgos en 4 secciones (datos / arquitectura / visualización / UX). Pedro revisó y priorizó; quedó accionable:
(1) esperar extracto Tezamat con ingresos tagueados; (2) separar anticipo (ejecutado, abajo); (4-6) forecast,
pulido y maestros cross-obra → cola. **Corrección de encuadre**: OC02 sin avance NO es un faltante (su etapa
no arrancó); "provisional" = solo la partición fiscal (Blanco/Negro/IVA/MEP default), no el avance/base (que
es real y reconcilia con el documento).

### Separación del anticipo (ejecutado — reader + frontend, Excel intacto)
El "certificado total" incluía el anticipo (>50%: $157,5M de $305M), inflando el headline. Se separó:
- [x] **`reader/cert.py`**: `_control_oc` lee la col `$ Anticipo certif.` (Cert_Control_OC!I); `read_cert`
      expone `anticipo`, `certificado_avance` (= total − anticipo) y `total_certificado` (incl., para
      reconciliación), por OC y total.
- [x] **`reader/read_obra.py`**: resumen agrega `certificado_avance` y `anticipo`.
- [x] **`web/js/ds.js`**: Dashboard card y Avance KPI pasan a **"Certificado de avance"** (sin anticipo) con
      el anticipo como línea aparte; tabla por OC con columnas **Anticipo** y **Cert. avance**; Reporte
      desglosa Anticipo ($157,5M) + Certificado de avance ($147,5M). El anticipo **no se pierde** del estado de la OC.
- [x] **Validado**: API `certificado_avance`=$147,5M / `anticipo`=$157,5M / total $305M; vistas HTTP 200;
      screenshots confirman las 3 (Dashboard, Avance, Reporte).
- Nota: el monto de anticipo mostrado ($157,5M) todavía es fiscal-provisional (base $120M grossed-up con
  CAC+IVA default); se vuelve real con la partición fiscal (pendiente A4).
